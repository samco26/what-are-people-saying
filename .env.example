# Copy to .env.local for local development. Nothing in version one reads a
# key yet: AI analysis and the three source connectors are later milestones.
# Keys belong only in .env.local, which git ignores, and in Vercel's own
# environment settings. Never in browser code and never in this file.
