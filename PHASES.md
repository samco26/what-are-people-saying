# What People Think — phase tracker

Last audited: 12 September 2026 (Australia/Melbourne).

## Where we are

**The website is built and deployed. Milestones 1–5 are complete; milestones 6–8 have successful live searches, with quality and cost checks still open.** The analysis backend uses OpenAI's Responses API and passes local type, build and sample-mode checks. Reddit's connector now passes simulated OAuth, search, comment, attribution and failure tests, but Reddit requires explicit Data API approval before live credentials can be used.

Use the ten milestones in [PROJECT.md](PROJECT.md) as the stable numbering. Completion is based on evidence, not the app's BETA version number.

## All phases

| Phase | Milestone | Status | Evidence / remaining acceptance check |
|---|---|---|---|
| 1 | Specification, development rules and private GitHub repository | Complete | PROJECT.md, AGENTS.md and secret exclusions exist. GitHub confirmed `samco26/what-are-people-saying` is private during the initial audit. |
| 2 | Produce and refine the visual mockup | Complete | Design record, original mockup checkpoint `2f3be72`, subsequent user-directed design passes and the implemented interface. Further polish belongs to phase 10. |
| 3 | Build the Next.js, TypeScript and Tailwind foundation | Complete | App pages, components, server route, TypeScript and Tailwind configuration exist. Production builds pass. |
| 4 | Connect GitHub to Vercel and deploy | Complete | Vercel's GitHub deployment record reported success for `ba269f2` on 11 September 2026. User also confirmed the site is live. |
| 5 | Complete search/loading/result flow with labelled sample data | Complete — sample mode | Six fictional samples, processing/result/error states and unknown-subject fallback are implemented. Desktop/mobile and route checks are recorded in README.md. Sample and unknown-subject API requests passed again after the OpenAI conversion. |
| 6 | Integrate server-side AI analysis | Live response verified; quality/cost checks pending | `src/lib/analysis/analyse.ts` uses the OpenAI Responses API, GPT-5.6 Luna by default, Zod Structured Outputs and `store: false`. TypeScript and the production build pass. Complete this phase only after a controlled request with synthetic evidence verifies account/model access, output quality, errors and measured cost. |
| 7 | Integrate YouTube | Live collection verified; quality/edge cases pending | Separate video/comment connector exists. Complete when an authorized request retrieves real relevant comments, preserves their links and yields a grounded answer; test disabled comments, quota errors and insufficient evidence. |
| 8 | Integrate X with collection and spending controls | Live collection verified; cost controls still need audit | After fixing X's ten-second `end_time` rule, a controlled production search returned 50 real X posts with source-specific analysis and links. The daily counter is only per server process, not a reliable whole-app spending cap. Reconcile actual charges and add an enforceable whole-app limit before completion. |
| 9 | Integrate Reddit when access is available | Implementation verified locally; approval/live verification pending | OAuth, post search, comment collection, attribution and partial-failure handling pass simulated tests. Current Reddit policy requires explicit approval even for non-commercial use. Submit the application, add the three server-only settings only after approval, then demonstrate a successful authenticated search with real evidence. |
| 10 | Refine UI and test privately before public availability | In progress | Substantial visual refinement and sample-mode checks are done. Still needs live-result quality checks, failure/partial-source tests, abuse/cost protections and a controlled user trial. |

## Immediate next checkpoint

Milestones 6–8 and 10: benchmark BETA 0.13.0 on controlled live searches for relevance, sentiment accuracy, summary/score consistency, latency and billed cost before calling those quality checks complete. The local audit and acceptance plan are in [SEARCH_AUDIT.md](SEARCH_AUDIT.md). Reddit approval and enforceable search/spending protection remain outstanding.

## Technology actually in the project

| Part | What is used | Where verified |
|---|---|---|
| Website and server routes | Next.js 15.5.25 | package.json; src/app |
| Interactive interface | React 18.3.1 | package.json; src/components |
| Application language | TypeScript | tsconfig.json; .ts/.tsx source files |
| Styling | Tailwind CSS 3.4.13 plus custom CSS | package.json; tailwind.config.ts; src/app/globals.css |
| Hosting | Vercel | Successful GitHub deployment status and user confirmation |
| AI provider in the code | OpenAI Responses API; GPT-5.6 Luna default | package.json; src/lib/analysis/analyse.ts; live production responses verified |

## Latest verification

13 September 2026: integrated GitHub commits through `98a3542` into BETA 0.13.0 before pushing. Preserved advisory fallback to the literal input, narrow named-review categories, compact default meter, Show more placement, phone spacing and removal of About this answer. Kept this audit's strict filtering and equal-evidence counts over the intervening reaction-weighted experiment. All 63 combined tests, TypeScript and the production build pass. Browser checks of the merged build pass at 1440×900, 390×844 and 320×660, including compact default meters, aligned star controls, source quotes, Back and reduced motion. The repository was reconfirmed private. Pushed merge `04c3a31` to private `origin/main`. GitHub reported Vercel Production deployment `6414614260` successful; the configured production homepage returned HTTP 200, the expected heading and BETA 0.13.0. The immutable deployment URL requires Vercel login. No paid live search, latency or cost evaluation was run.

BETA 0.13.0 is **verified locally, not deployed**. The user scrapped category-specific layouts: all five categories now share the default answer; the four review categories use a star pill level with the platform icons. All results print the interpreted subject; duplicated quote headings are removed; suggestions pulse subtly and remain accessible. The search audit found estimated overall sentiment, unclassified evidence leakage, unrelated keyword OR matches and overly broad opinion drilldowns. The backend now merges cited identity extraction with query planning, classifies every collected opinion before synthesis, derives scores from accepted unique evidence, enforces classification completeness and post-filter sufficiency, and passes only checked findings to the final writer. The successful path still uses four AI requests. All 63 tests, TypeScript and the production build pass. Simulated browser checks pass for all five categories at 1440×900, 390×844 and 320×660, including aligned controls, no page/row overflow, Back, duplicate quotes, clickable glow and reduced motion; no runtime errors. The tests/build used bundled Node.js 24; browser checks used bundled Playwright because agent-browser is absent. No live quality, latency, cost or new deployment success is claimed.


BETA 0.12.8 answers four small requests. On the usual answer the platform buttons and the sentiment bar share one line: `Answer.tsx` wraps them in `.source-row`, the bar takes the remaining width and `SentimentBar`'s new `compact` flag drops the legend (the platform view keeps its legend); the insufficient state draws the same row with the empty grey bar. Show more opinions, previously a 12px near-white link floating at the foot of the screen and easy to miss, is now a text link under the card beside How it works, rendered by `SearchCard` for a general result with more than six opinions and hidden below 1050px where every pill is listed; `OpinionPills` loses its `onMore` prop. On phones `.search-home[data-result="true"]` gets 18px of top padding so the heading clears the top of the screen once the header is hidden, and the landing state gets 66px of bottom padding so the search field, not the heading-plus-card block, sits at the screen's centre. Measured in the harness: at 1280 by 800 with a forced nine-opinion general result the buttons (44px) and the bar (12px) were vertically centred on one line, six pills were visible, and Show more opinions opened the full list of nine; at 390 by 740 and 390 by 660 the field's centre was 370 and 330, the screen centres; after a search the heading sat 26px from the top and the bar beside the buttons. The 8px scroll the harness still reports inside the card is its entry animation held at the first frame, which the pane does not run to completion. Not run under Node.js here.

BETA 0.12.7 follows three more requests from the user. The sentiment bar is removed from the four category cards (`Foot` and the card's `SentimentBar` import are gone from `CategoryCard.tsx`); the usual answer keeps it. On phones `PhoneCard` holds only the summary and the rating strip, and `SearchCard` renders `OpinionPills` for a category result as well as a general one when `usePhone()` is true, so the opinions sit under the card as the same scrolling list with no heading; the phone-only opinion box, its CSS and the card-fills-the-screen rules are removed. The About this answer link, the `about` view and `SubjectFacts.tsx` are deleted, with the matching test assertions and harness entry; the fact-check itself still runs and feeds the plan and analysis. Two verification fixes ride along: `usePhone` listens to `resize` as well as the media query, and the harness's `__size` dispatches a `resize` event into the frame, because the pane fires neither resize nor matchMedia change events when the frame is restyled (which is why the first phone check showed no pills). Measured in the harness at 390 by 740: the product card ends at 300px and the app card at 359px, the pill list fills the rest of a 740px page and scrolls in place when it must, the header is hidden and no bar is drawn; the Tuscany answer keeps its bar; at 1280 by 800 the product card keeps its opinions inside with no bar and no overflow. Not run under Node.js here.

BETA 0.12.6 reshapes the category cards at the user's request. `src/components/CategoryCard.tsx` now has one desktop skeleton (`DesktopCard`) and one phone column (`PhoneCard`) sharing a `RatingStrip`: a compact pill-shaped tile (score, stars, approval share and opinion count) with the three platform buttons beside it, `align-items:stretch` making the buttons the tile's height. `PlatformStack.tsx` is logo buttons only; the per-platform star ratings, the tick, cross and dash marks on chips and the small stars on review rows and cards are removed, and the CSS for the old two-column, grid and side layouts with them. The scene carries `data-searched` once the card unfolds and a `:has()` rule hides the phone header from then on. Measured in the harness: at 1280 by 800 the product, place and app cards put the tile and buttons at one 54px height with no card overflow; at 390 by 740 the header is gone, the strip is 57px with the approval line wrapping inside the tile, and the page stays 740px. Not run under Node.js here. The film card still has no sample.

BETA 0.12.5 follows the user's live search for "Melbourne", which returned "Let's narrow down the subject": the 0.12.2 web fact-check had judged the name ambiguous (two cities) and `src/app/api/consensus/route.ts` stopped before collecting anything. The fact-check is now advisory. A resolved lookup still supplies the official name, aliases and dated facts to the plan, the collection and the analysis, and marks the name confirmed so the plan does not flag it again; an ambiguous, unverified or failed lookup lets the search run on the subject as typed with no context, exactly as it ran before the lookup existed, with did-you-mean still available. The `subject-unresolved` response kind, its screen and its tests are removed; the replacement route test drives an ambiguous lookup through plan, collection and analysis and asserts no context, no confirmed note and the usual answer. How it works says the same. Not run under Node.js here; the harness parsed 37 files and mounted the app. The live check is "Melbourne" returning the usual answer, then the 0.12.4 category cases.

BETA 0.12.4 tightens the category rule at the user's request, after live searches put broad subjects on cards. The plan's instructions (`src/lib/searchPlan.ts`) now give a category other than general only for one specific named thing with its own listing on the site people check for that kind of thing, with the boundary cases spelled out: a named product or model, including a rumoured one, but not a brand alone or a class of things; a named venue or chain, but not a city, country or region; a named film, series, book, album or game, but not a franchise, genre or artist; a named app or service, but not the company behind it or a category; and any phrase that adds a condition, time, use, aspect or comparison to a thing is general, as is doubt. The suggestion's category follows the same test. The "living in Melbourne" sample lost its place label and a clearly fictional restaurant chain, Harbour Lane Burgers, was added as the place demo; it joins the example list but not the rotating heading. A new plan test asserts the rule and each exclusion is in the instructions. Not run here (no Node.js); the harness parsed all 37 files, mounted the app, and drew the new place card and the relabelled Melbourne answer. The live check is whether the model applies the rule to real names: "smartphones", "Rome", "Apple" and "McDonald's breakfast" should get the usual answer and "iPhone 17" and "McDonald's" a card.

BETA 0.12.3 follows the user's review of the first live category-card results. Three things were wrong with them. The summary described the opinions from the outside ("Opinion leans positive toward Melbourne, especially its beauty...") and spent its second sentence on sampling caveats; the prompt now asks for the voice of a person telling a friend what people think, subject first and views stated directly, with the caveats confined to confidence. The star rating for "iphone fold" read 2.0 although the subject is well liked; on review, the overall bar and the stars came from a separate sentiment estimate the model wrote before it classified anything, while the platform bars came from the classification, so the two could disagree and the estimate could be swayed by the sheer number of drive-by complaints. The estimate is gone from the schema; the overall split, the stars and the platform bars are now all counted from the per-entry labels in `src/lib/analysis/evidence.ts`, each entry weighted 1 + log10(1 + reactions) so a comment a thousand people liked counts four times a comment nobody noticed and a viral one at most about six, and the classification rules now define the four labels around the entry's view of the subject rather than its tone (a dig at a rival or a joke is irrelevant, blunt praise is praise). Each source status carries `relevant`, the count of entries labelled as about the subject, and the cards print that. Third, a subject that does not exist or is too specific still got a card with a rating; the route now returns the insufficient state when fewer than `MIN_ITEMS` on-topic entries survive classification, and `Answer.tsx` draws that state as the usual answer with a plain sentence, the platform buttons greyed and an empty grey bar (`SentimentBar` with no split), never a card or a star. On phones the rating tile moved from the top of the column to directly above the platform row, fixed like it. Not run here (no Node.js): the tests, which gain cases for the weighting, the counted split and the relevant counts and were updated for the removed estimate and new prompt wording; the harness parsed all 34 files and mounted the app, and gained a `__respond(body)` helper used to draw the insufficient state. The live re-check of "iphone fold" and of a place summary's voice are the checks to make.

BETA 0.12.2 merges the subject fact-check with GitHub’s BETA 0.12.1 category cards, search planner and collection improvements, within milestones 6 and 10. Verified spelling, aliases and cited dated facts reach planning, collection, category labels and analysis; uncertainty stops before platform reads. All 60 combined tests, TypeScript and the production build pass. Desktop (1440×900) and phone (390×844) browser checks confirm category cards, verified naming, cited facts, Back, ambiguity and retry without runtime errors or page overflow, using explicitly simulated data. GitHub reports Vercel deployment success for merged commit `3870bd8`; the production homepage returned HTTP 200 and BETA 0.12.2. The repository was confirmed private. No paid live lookup, measured cost or new live result-quality verification is claimed.

BETA 0.12.1 follows the first live use of the category cards. Searching "iphone duo" was flagged ambiguous with the suggestion "iphone duo concept", and accepting the suggestion re-planned it as general, so both searches got the usual answer. Two fixes: the plan's ambiguity rule now says a name is ambiguous only when shared by genuinely different things (Jaguar the car and the animal), never a rumoured, concept or unreleased product or another generation of one product; and the plan returns `suggestionCategory` alongside the suggestion, the browser sends it back as `categoryHint` when the suggestion is accepted, the plan is then told the reading is confirmed and must not flag it again, and the route falls back to the hint if the plan still answers general. A confirmed search is never offered another suggestion. The collection still runs fresh for the accepted subject; only the category is carried over. Copy: the heading is "Find the popular opinion on", the rotating examples lose their question mark, and on phones the header shows Privacy on the left instead of the title. Not run here (no Node.js): the tests, which gain cases for the suggestion category and the confirmed-reading prompt; the harness parsed and mounted the app. The live classification of real subjects remains the check to make.

BETA 0.12.0 adds the category cards the user refined through the "Category Cards" mockup on 12 September 2026. The search plan (`src/lib/searchPlan.ts`) gains four fields: `category` (film, product, place, app or general), a short `kind` line, an `ambiguous` flag and a `suggestion`; the route copies them onto the result, and an ambiguous name is always answered with the general card plus the suggestion, never a guessed category. `src/lib/stars.ts` turns a sentiment split into a star rating: the positive share of opinionated posts, mapped through anchors pinned to the Rotten Tomatoes thresholds (60% is 3.5 stars, 75% is 4), pulled toward 3 when neutral posts are most of the sample or the sample is small. `src/components/CategoryCard.tsx` draws the four desktop layouts and one shared phone column; `PlatformStack.tsx` puts each platform's own rating beside its logo; recurring opinions now carry their support count (`support` on `RecurringOpinion`, which the analysis already computed and previously dropped). Two samples are labelled with a category (the Keychron K2 as a product, living in Melbourne as a place, the newest ChatGPT model as an app) so the sample mode shows the cards; there is no film sample. This machine has no Node.js, so `npm test`, `npm run typecheck` and `npm run build` could not run; the no-Node harness parsed all 34 source files and mounted the app, and the three sample cards were checked at 1280 by 800 (no card scroll, General view returns to the usual answer) and at 390 by 740 (card fills the screen, only the opinions box scrolls, no General view button). New tests cover the plan fields and the star anchors, neutral handling, small-sample shrinkage and a worked example, but have not run. Outstanding: the Vercel build, and a controlled live search to see how the plan classifies real subjects, how often it flags ambiguity, and whether the kind line is accurate.

BETA 0.11.0 adds a search plan before collection and changes X's ordering. One small OpenAI call (`src/lib/searchPlan.ts`) turns the typed subject into the phrases, keywords, exclusions and YouTube words that discussion would actually contain; the platform queries are assembled in code with all operator characters stripped from the model's output, a short subject as typed is always kept among the X terms, and any failure falls back to searching the subject as typed. X is asked for `sort_order=relevancy` (same per-post price). The plan's one-sentence interpretation is passed to the analysis as "Taken to mean". Two smaller changes ride along: X's per-process daily budget now counts only posts actually returned (empty and failed searches give their 20-post reservation back; the enforceable ceiling is the monthly spend limit in X's developer console, which is a manual setting outside the code), and YouTube fills each video's 30 places with earlier selections first and then the most-liked of the top and recent comments, with no minimum. New tests cover planning, fallback, sanitising, length limits, planned X terms and the relevance order, the adaptive passes carrying the queries, the budget refunds and the liked-comment preference. The docs and badge for this version reached `origin/main` in commit `b4ae406` ahead of the code, because a concurrent session committed the working tree; the code followed in `f67cb04`. Vercel deployed `f67cb04` (the live bundle carries this version's changelog text) and a controlled production search for "the weather in Tuscany in August" returned a full result in 7.8 s: `Server-Timing` reported plan 1.4 s, collection 0.7 s and analysis 5.3 s; 70 YouTube comments from 10 videos, 6 X posts from the last 3 months for a plain-English subject that the old exact-phrase query would not have matched, Reddit not connected, four recurring opinions. The budget refund and liked-comment preference are in the same build but are not separately observable from outside. `npm test`, `npm run typecheck` and `npm run build` still have not run on a machine with Node.js. This machine has no Node.js, so `npm test`, `npm run typecheck` and `npm run build` could not run; the no-Node harness parsed all 30 source files and mounted the app, and the query-building cases from the tests were executed in the harness page with the expected results. Live effect on match rates, the extra plan latency (expected one to two seconds) and the Vercel build are the outstanding checks.

BETA 0.10.3 trims the analysis output without changing the sample, the model or the summary rules: the classification comes back as four lists of reference numbers, per-platform readings are reduced to the representative references the screen uses, and YouTube reads each video's top and recent comments together. The answer card shows a small note above the sentiment bar when fewer than 50 opinions were analysed. On phones the full opinion list scrolls within itself under the answer. The mocked tests were updated for the new shapes but could not run here (no Node.js); the harness parsed and mounted the app, and the phone and desktop layouts were checked in it. Three pushes failed to build because a concurrent session's edit to youtube.ts (reading a new queries option) was committed before its CollectOptions field; committing that three-line field fixed the build. Vercel deployed 8a53405 and a controlled production search for "Mercury Marine" returned a full result: 258 opinions (243 YouTube, 15 X), 255 of them classified, two platform readings and 8 recurring opinions with evidence. Server-Timing reported 1.03 s collection and 11.95 s analysis against about 11.5 s for the same subject before the change, so the leaner output did not measurably shorten the analysis on this sample; the model's response time, not the output size, dominates. No social content was persisted.

BETA 0.10.2 centres the BETA label text and returns the How it works link to the bottom right of the search card, moving the sixth desktop opinion pill outward so the two do not overlap. Checked in the no-Node harness at 1914 by 920; the Vercel production build is the outstanding check.

BETA 0.10.1 applies the user's small adjustments: the About this answer panel and the list behind the BETA badge are removed (the badge is now a plain label), the overall sentiment bar sits under the platform icons in place of the coverage note, the arrows on the How it works and Show more opinions links are gone, two sentences are dropped from How it works, and a tap or click anywhere outside an expanded view closes it. The answer card keeps its scrollbar hidden while it unfolds so the scroll track no longer flashes, and phone opinion pills rise in vertically only, which removes a brief horizontal scroll line. On phones the heading fits on one line and the answer text and spacing are tighter so the three opinion pills fit on a 390 by 660 screen; on desktop the first pill sits above and left of the heading and the How it works link is centred so it clears the lower pills. This machine has no Node.js, so `npm test`, `npm run typecheck` and `npm run build` could not run here; the no-Node harness (updated to load the current modules) parsed all 29 source files and mounted the app without runtime errors, and the browser checks above were made in that harness with a labelled fictional sample. Vercel blocked the first push of this version because the commit was authored with an email not linked to the GitHub account; this follow-up commit uses the linked no-reply identity, and the repository now sets that identity locally. The Vercel production build for this commit is the outstanding check. No live integration is claimed.

BETA 0.10.0 implements the user-refined liquid-glass UI in phase 10: fixed-screen mobile details, recurring opinion pills, icon-only source controls, full-width platform meters and grouped original posts with sentiment-coloured verbatim excerpts. Counts stay internal. Thirty-five mocked tests and the production build pass. Desktop/mobile browser checks verified source replacement, opinion drilldown, Back/Escape, loading/error/unknown states and no document overflow. The expanded structured analysis contract has not been checked with a paid live request; its quality, larger output budget, latency and cost remain to be validated. The newer Reddit activation, attribution and privacy-page changes have been preserved. This checkpoint does not claim a new live integration success.

BETA 0.9.5 makes Reddit activation require the client ID, client secret and identifying user agent together. Simulated tests verify the OAuth token exchange, bounded search and comment requests, exact date filtering, evidence links, required public username attribution and explicit partial failures. The analysis prompt still excludes author names. A privacy page accurately describes transient processing and external providers. Thirty tests, TypeScript and the production build pass. Vercel reported the `6f354ed` deployment successful; the production homepage showed BETA 0.9.5 and `/privacy` returned HTTP 200. Live Reddit access remains unverified pending explicit approval.

X now uses official full-archive search with its own 3/12/36-month empty-result fallback, even when other sources have sufficient evidence. It stops at the first nonempty response and keeps the 20-post total cap. Twenty-seven tests and the production build pass, including the 12-month and 3-year paths, empty results, access/billing failures, timeout cancellation, per-request reuse, and the wider answer window. Vercel deployed `122d279` successfully. The live exact query “mercury marine boat engines australia” completed all three X windows with no matches and returned 159 YouTube opinions (9.1 seconds total; 2.840 seconds collection). “Mercury Marine” returned 15 X posts over three months and 243 YouTube opinions (12.5 seconds total; 1.020 seconds collection), confirming archive access and stopping on matches. No social content was persisted. Earlier successful X searches below used the recent endpoint.

The preceding checkpoint capped X at 20 posts per search, at most US$0.10 in post-read charges at the published US$0.005 rate, excluding AI. The code clamps old higher environment settings to 20. Twenty-two tests and the production build passed; a controlled production search returned exactly 20 X posts. Earlier live 50-post checks below describe the previous limit.

BETA 0.9.4 adds 3/12/36-month collection with a 50-opinion expansion threshold, keeps earlier evidence, and reuses per-request source responses. Twenty-one mocked tests pass, including X's present-time request rule, accepted empty responses and safe provider-error handling. TypeScript and the production build pass. Vercel deployed the X fix in `b466fbe`; the Mercury Marine request then succeeded without HTTP 400 but found no exact-phrase X matches in seven days. A subsequent production “ChatGPT” search returned 234 YouTube comments and 50 X posts with live links and separate source analysis. This verifies nonempty paid X collection; charges still need to be reconciled in the provider dashboards. Reddit was unconnected, and no social content was saved during verification.

The BETA 0.9.3 search and daily-example work has been combined with the OpenAI migration. Both consensus and news-topic selection now use the Responses API with `store: false`. Ten mocked tests, TypeScript and the production build pass. GitHub/Vercel confirmed production deployment `6403248138` for commit `a39030b` succeeded on 12 September 2026. The live homepage returned HTTP 200 with BETA 0.9.3 and the new niche example; `/api/subjects` returned HTTP 200 with six news topics and six evergreen examples. This verifies deployment and live news suggestions, not the paid consensus/YouTube analysis or its speed; those milestone acceptance checks remain open.

- `npm run typecheck`: passed.
- `npm run build`: passed with Next.js 15.5.25.
- Production server sample request: passed without an AI key.
- Production server unknown-subject request: passed and returned the truthful no-live-search state.
- Dependency audit: zero known vulnerabilities.
- No Anthropic or Claude references remain in active project files or the dependency lock.
- No credentials were read or created locally. The controlled live query used the production server’s configured keys.

## Keeping this live

At each relevant checkpoint, update this file with verified evidence and refresh the in-conversation tracker. The tracker is updated on request or during project work; it is not an automatic background monitor. Never mark a live integration complete merely because its code exists.
